# Topology Visualiser

Purpose: render persisted topology state in the terminal.

```sh
cargo run -p void-cli -- topology
```

Initial output is ASCII. The same topology data can later feed a runtime-native visualisation surface.

